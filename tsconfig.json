{
    "compilerOptions": {
      "target": "ES2021",      // ES2021 기능들을 지원합니다.
      "module": "CommonJS",   // Node.js는 여전히 CommonJS 모듈을 사용합니다.
      "moduleResolution": "node", // Node.js 방식의 모듈 해석을 사용합니다.
  
      "strict": true,         // 모든 엄격한 타입 검사 옵션을 활성화합니다.
      "esModuleInterop": true, // ES 모듈과의 상호 운용성을 위한 설정입니다.
  
      "skipLibCheck": true,   // 선언 파일의 타입 검사를 건너뜁니다.
      "forceConsistentCasingInFileNames": true, // 대/소문자 일관성을 강제합니다.
  
      "outDir": "./build",     // 컴파일된 JavaScript 파일이 저장될 디렉토리입니다.
  
      "resolveJsonModule": true, // JSON 모듈을 import할 수 있게 합니다.
  
      "types": ["node"],      // Node.js 타입 정의를 사용합니다.
      "lib": ["ES2021"],      // ES2021 라이브러리 기능을 포함합니다.
  
      "experimentalDecorators": true, // 데코레이터를 사용하려면 이 옵션이 필요합니다.
  
      "isolatedModules": true // 각 파일을 별도의 모듈로 간주합니다.
    },
    "include": ["index.ts", "src/**/*.ts"], // TypeScript 파일이 있는 위치입니다.
    "exclude": ["node_modules"] // node_modules 디렉토리를 제외합니다.
  }